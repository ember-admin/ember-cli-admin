each action in controller.additionalActions
  view 'base/actions/action' tagName="button" href="#" contextBinding="action" modelBinding="item" titleBinding="action.title"
    span{{bind-attr class="action.iconClass"}}