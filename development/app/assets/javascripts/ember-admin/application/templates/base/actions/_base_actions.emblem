each action in controller.actions
  Admin.Base.Views.ActionView tagName="button" href="#" contextBinding="action" modelBinding=item titleBinding=action.title
    span{{bindAttr class=action.iconClass}}