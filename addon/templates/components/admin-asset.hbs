{{#if isImage}}
  <a class="thumbnail" href="#">
    <img src="{{asset.thumb_url}}" {{action "openImagePreview" asset.url on="click"}}>
  </a>
  <a class="btn btn-danger btn-xs delete-asset" {{action "deleteAsset" on="click"}}>x</a>
{{else}}
  <a class="thumbnail">
    <img src="/assets/images/fileicon.png">
  </a>
  <a class="btn btn-danger btn-xs delete-asset" {{action "deleteAsset" on="click"}}>x</a>
  <div class="caption">
    <h5>{{asset.original_filename}}</h5>
  </div>
{{/if}}
