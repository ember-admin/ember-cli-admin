// Generated by CoffeeScript 1.8.0
import Ember from 'ember';
import BaseActionsMixin from 'emberadmin/mixins/controllers/base-actions';
import FileUploadMixin from 'emberadmin/mixins/controllers/fileupload';
import AttributesMixin from 'emberadmin/mixins/controllers/attributes';
import PaginationMixin from 'emberadmin/mixins/controllers/pagination';
import BatchActionsMixin from 'emberadmin/mixins/controllers/batch-actions';
import FormActionsMixin from 'emberadmin/mixins/controllers/form-actions';
import BaseDecorator from 'emberadmin/decorators/base-decorator';
var baseMixin;

baseMixin = Ember.Mixin.create({
  decorator: (function() {
    if (this.get('model')) {
      return BaseDecorator.create({
        model: this.get('model')
      });
    }
  }).property('model'),
  _name: (function() {
    return this._debugContainerKey.split(":")[1].replace(/(\/[Ss]how)|(\/[Ee]dit)|(\/[Nn]ew)/, '');
  }).property()
});

baseMixin.reopen(BaseActionsMixin, FileUploadMixin, AttributesMixin, PaginationMixin, BatchActionsMixin, FormActionsMixin);

export default baseMixin;
