<div class="modal show">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" click="closeModal" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        <h4 class="modal-title">{{modalObject.actionData.title}}</h4>
      </div>
      <div class="modal-body">
        <p>{{modalObject.actionData.confirm}}</p>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-default" data-dismiss="modal" {{action "closeModal" on="click"}}>Close</button>
        <button type="button" class="btn btn-primary" {{action "confirm" on="click"}}>Confirm</button>
      </div>
    </div>
  </div>
</div>