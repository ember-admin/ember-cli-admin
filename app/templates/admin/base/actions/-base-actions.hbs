{{#each action in itemActions}}
    {{#view 'admin/base/actions/action' tagName="button" href="#" context=action model=item title=action.title}}
        <span class="{{action.iconClass}}"></span>
    {{/view}}
{{/each}}
