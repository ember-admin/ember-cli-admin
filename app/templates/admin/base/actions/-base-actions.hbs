{{#each itemActions as |action|}}
    {{admin-action
    tagName="button"
    actionName=action
    model=item
    title=action.title
    adminAction='adminAction'
    newAction=newAction
    allActions=allActions}}
{{/each}}
