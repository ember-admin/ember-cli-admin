{{#each additionalActions as |action|}}
    {{admin-action
    tagName="button"
    actionName=action
    model=item
    title=action.title
    adminAction='adminAction'
    newAction=newAction
    itemActions=itemActions}}
{{/each}}

{{#if item.additionalActions}}
    {{#each item.additionalActions as |action|}}
        {{admin-action
        tagName="button"
        actionName=action
        model=item
        title=action.title
        adminAction='adminAction'
        newAction=newAction
        itemActions=itemActions}}
    {{/each}}
{{/if}}
