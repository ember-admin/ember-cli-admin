<div class="navbar navbar-inverse navbar-fixed-top">
  <a class="navbar-brand" href="{{titleLinksTo}}">{{siteTitle}}</a>
  <ul class="nav navbar-nav">
      {{#each content as |navigation|}}
          {{partial "admin/navigation/item"}}
      {{/each}}
  </ul>
</div>
