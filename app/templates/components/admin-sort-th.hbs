{{#if isInSortFields}}
  {{#if isOrderAscending}}
      {{attributeName}} ▲
  {{else}}
      {{#if isOrderDescending}}
          {{attributeName}} ▼
      {{else}}
          {{attributeName}} ▲▼
      {{/if}}
  {{/if}}
{{else}}
  {{attributeName}}
{{/if}}