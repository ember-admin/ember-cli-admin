each action in controller.additionalActions
  Admin.Base.Views.ActionView tagName="a" href="#" contextBinding="action" modelBinding=item titleBinding=action.title
    i{{bindAttr class=action.iconClass}}