each action in controller.actions
  Admin.Base.Views.ActionView tagName="a" href="#" contextBinding="action" modelBinding=item titleBinding=action.title data-confirmBinding=action.confirm
    i{{bindAttr class=action.iconClass}}